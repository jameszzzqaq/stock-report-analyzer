---
name: guigui-investment-strategy-v015
description: 系统化价值型/估值回归型选股分析框架。通过四因子递进筛选执行多阶段分析，用于港股、A股和美股标的的价值判断、穿透回报率评估、现金质量审计和安全边际评估。
---

# 龟龟投资策略 v0.15 — Skills 入口

> 本 Skill 为多阶段投资分析框架。请按下方索引按需加载模块文件，无需一次性读取全部内容。

---

## 快速开始

**用户输入**：股票代码/名称（必需）+ 持股渠道（可选）+ PDF 年报（可选）

**执行入口**：阅读 `00_coordinator.md` 获取完整调度流程。

**环境前置**：
- 需要可运行 `python`
- Phase 1 依赖 `yfinance`
- 若依赖未安装，先执行 `python -m pip install -r {strategy_dir}/scripts/requirements.txt`

---

## 模块索引（渐进式加载）

按执行顺序排列，仅在需要时加载对应文件：

| 序号 | 文件 | 用途 | 何时加载 |
|:----:|:-----|:-----|:---------|
| 0 | `00_coordinator.md` | 阶段调度、输入解析、异常处理、单位规则 | 启动时必读 |
| 1 | `01_phase1_数据采集.md` | 市场数据 + 财务报表 + 行业信息采集 | 执行 Phase 1 时 |
| 2 | `02_phase2_PDF解析.md` | 年报 PDF 结构化数据提取 | 执行 Phase 2 时（有 PDF 才加载） |
| 3 | `03_strategy_knowledge_base.md` | 策略核心思想、四因子概述、行为规范 | 进入 Phase 3 时必读 |
| 4 | `04_factor1_定性分析.md` | 因子1A 快筛 + 因子1B 深度定性（9 模块） | 执行因子1 时 |
| 5 | `05_factor2_粗算.md` | 穿透回报率粗算（Top-Down） | 因子1通过后 |
| 6 | `06_factor3_精算.md` | 穿透回报率精算（Bottom-Up）+ 现金审计 | 因子2通过后 |
| 7 | `07_factor4_估值.md` | 门槛比较 + 安全边际 + 仓位建议 | 因子3通过后 |
| 8 | `08_report_template.md` | 最终报告输出模板 | 生成报告时 |
| 9 | `09_execution_workflow.md` | 执行步骤索引（协调器快速参考） | 需要回顾执行步骤时 |

> Token 节省说明：如果标的在因子1A被否决，只需加载序号 0-4 的文件。

---

## 环境变量约定

| 变量 | 含义 | 示例 |
|:-----|:-----|:-----|
| `{workspace}` | 用户工作空间根目录 | 当前工作目录 |
| `{stock_code}` | 目标公司的标准计算代码 | `0001.HK`、`AAPL` |
| `{symbol}` | 目录名，通常为去掉交易所后缀的代码 | `0001`、`AAPL` |
| `{company_name}` | 公司的中文名/惯用名 | `长和`、`腾讯控股` |
| `{strategy_dir}` | 本 Skill 目录路径 | 本文件所在目录 |
| `{user_upload_dir}` | 用户上传文件目录 | 由 AI 环境自动确定 |

---

## AI 平台兼容性说明

本 Skill 设计为跨平台通用：

| 平台 | WebSearch | PDF 解析 | 脚本执行 | 文件读写 |
|:-----|:---------|:---------|:---------|:---------|
| Gemini | 视环境而定 | 视环境而定 | 可执行本地脚本 | 可用 |
| Claude Code | 视环境而定 | 视环境而定 | 可执行本地脚本 | 可用 |
| OpenAI Codex | 视环境而定 | 视环境而定 | 可执行本地脚本 | 可用 |

> 执行环境要求：Phase 1 通过本地执行 `scripts/fetch_market_data.py` 获取 yfinance 数据；搜索和 PDF 能力按当前平台提供的工具降级适配。

---

## 否决门机制

框架内置多级否决门，以下任一触发即停止后续分析：

1. 因子1A 否决：6 项快筛任一不通过。
2. 因子1B 否决：管理层判定“损害价值”。
3. 因子2 分配能力否决：自由现金流长期为负。
4. 因子2 粗算否决门：R < Rf 或 R < II×0.5。
5. 因子4 排除：安全边际不足且价值陷阱风险高。

---

*龟龟投资策略 v0.15 | Skills 入口 | 通用 AI 平台兼容*